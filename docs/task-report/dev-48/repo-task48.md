# Task 48 Report — OPEN-GAPS.md 定稿

- 关联任务:`openspec/changes/retire-stale-root-docs/tasks.md` §1(1.1–1.5)
- 涉及文件:`docs/OPEN-GAPS.md`
- 日期:2026-09-18

## 目的

让 `docs/OPEN-GAPS.md` 在退休前先自洽——消除"逐条说已处理、汇总说未处理"这处自相矛盾,避免把一份自打嘴巴的文档冻进历史。

## 作用

1. 末尾汇总表的 OG-5~OG-8 四行按各条实测注记改写(任务 1.1):状态列从"**不符**""spec 未覆盖"改为已收口/已删除/已落地/已换判据;建议列从将来的"扩白名单 + 接 `autoConfirm`""随 OG-5 接线"改为已采纳的方案,与 OG-1~OG-4 的行格式一致。
2. 末尾导语从只宣布 OG-1~OG-4 扩写为两个 change 的完整归属(任务 1.2):OG-1~OG-4 归 `harden-local-identity-and-align-admission-spec`,OG-5~OG-8 归 `stabilize-candidate-auto-admission`。
3. 删掉两段已失效的后续动作指引(任务 1.3):"要落地其中任何一条:OG-1/OG-2/OG-5/OG-6 需要开 OpenSpec change"与"OG-5~OG-8 之间有顺序依赖:OG-8 的方案 B/C 决定要在前"——它们描述的是已走完的执行顺序。
4. 显式记录余留的能力边界:OG-1 的项目级配置层、OG-2 的证据累积仍未实现,但主规范已改成不承诺它们,因此属**终态**而非待办。
5. 在字段约定处加一行消歧:"建议采纳"与"何时该做"记录的是发现当时的建议,不是最终决定。

## 特点

- 逐条正文的"建议方案 / 建议采纳 / 何时该做"保持原文不改:它们记录的是决策**当时**的判断,与"处理结果"并置才构成完整事实。改写它们等于篡改当时的推理痕迹。
- 消歧放在字段约定处,而不是逐条改写——一处说明解决全部八条的误读风险。

## 边界

- 只动汇总表、导语与失效指引;八条 OG 的证据、影响、建议三段一字未改。
- 本 change 新发现的 `.gitignore` 规则缺陷(声称忽略 `docs/archive/`、`docs/task-report/`、`docs/evaluation-reports/`,而这三棵目录共 116 个文件已入库)未登记为 OG-9,由独立 change 承接。
