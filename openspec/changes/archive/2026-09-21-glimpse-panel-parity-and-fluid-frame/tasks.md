# Tasks — Glimpse 窗口与终端面板对齐

> 本变更的代码已在 `feat(glimpse)` 提交中落地；这里记录的是**契约对齐**工作与它的证据。
> 真实窗口上的两项人工确认（控件交互、窗口拉伸）是发布后的人工步骤，不属于本变更的任务清单，见 proposal 的 Impact 段。

## 1. 契约对齐

- [x] 1.1 复核 `openspec/specs/glimpse-console-panel/spec.md` 的 12 条要求，逐条判定本次行为改动是否触及（结论：几何、语言两条需改；「同等操作入口」一条已被实现补充）
- [x] 1.2 几何要求：正文改为「启动尺寸 800×600 + 内容铺满可缩放的宿主窗口」，保留「窗口不得自行增长 / 超长区域内部滚动」，并新增 resize 场景（要求名称保持不变，避免 MODIFIED 被当作改名）
- [x] 1.3 语言要求：明确覆盖由已存数据派生、但由代码固定的文案（候选理由/证据说明、审计事件名称与状态词），并写明不得改写已存数据来达成
- [x] 1.4 新增要求：窗口的设置字段必须可编辑且沿用终端面板的守卫（枚举 → 选择器、自由文本 → 输入框、环境变量钉住 → 只读、无独立保存步骤、动作行需确认且不写配置）
- [x] 1.5 `TUI-DESIGN.md` §1/§2A 与 `docs/` 口径同步（§2A 的 `frameless: true` 与代码不符，一并更正）

## 2. 验证

- [x] 2.1 `openspec validate` 通过（变更本体 + 全仓 change 集合）
- [x] 2.2 门禁：`pnpm typecheck`、`pnpm -w run lint`、`pnpm test` 三条全绿（1042 passed / 8 skipped）
- [x] 2.3 逐条比对：delta 的 3 条要求在同步后的主 spec 中可读出，且未改动的要求与场景保持原样
