## 1. 决策 runner 边界(零消费方先落地)

- [x] 1.1 `config.ts` 新增 runner 总开关与三个消费方子开关(全部默认 off),验证默认配置下单元测试断言"零网络调用"路径成立
- [x] 1.2 新建 `src/decision/` 装配点:provider-neutral 接口 + 裸 HTTP `POST /v1/systemone` 实现(Choice/Score/Noul 三原语封装),验证注入替代 runner 时默认实现不被调用的单测
- [x] 1.3 串接出站安全:state 先过 `prepareExternalContent`,无法确认安全拒绝外发;key 仅读环境变量,验证含凭据样例被脱敏、日志与诊断无 key 的测试
- [x] 1.4 串接入站回筛:回答过注入判定与可持久化判定,不合格丢弃;调用有 AbortSignal 时间/字符预算,验证超时中止 + fail-open 回退 + 有界失败计数的单测
- [x] 1.5 status/doctor 增加决策接入报告(开关状态、调用/失败/门控计数,无正文),验证输出有界且不含 state/回答正文

## 2. 门控精排(消费方一)

- [x] 2.1 实现粗排头部"分差 ≤ 阈值"门控(固定阈值,配置可调),验证分差大跳过分差小触发、跳过计数 +1 的单测
- [x] 2.2 精排只重排头部 K 条:锁定结果成员与条目/字符预算,验证重排前后集合相等的单测
- [x] 2.3 失败回退:精排失败/超时/回筛丢弃时返回粗排原始顺序,验证与开关关闭时输出逐字节一致的回归测试

## 3. 重复 prompt 稳定性判定(消费方二)

- [x] 3.1 从 L0 事件日志实现同义重复确定性计数(复用 candidate-digest/embedding 近似,默认阈值 3),验证纯计数零模型调用的单测与跨会话累计场景
- [x] 3.2 达阈值后请求一次 Noul 稳定性判定:≥ 概率阈值生成**待审候选**(证据类型为重复信号 + L0 来源引用),验证候选不自动写入 T1、低概率丢弃仅留有界计数的单测
- [x] 3.3 runner 关闭/失败时不生成候选也不放宽准入,验证行为与规则不存在时一致的单测;挫败型重复样例(重复报错追问)判定为低概率的用例测试

## 4. 置信度校准标注(消费方三)

- [x] 4.1 校准输出写入候选 `confidence` 并标 `calibrated` 来源(不新增 evidence type),验证来源不伪装为 `explicit-user-statement` 的单测
- [x] 4.2 校准不可用时保留原值原标注;验证既有 `admission-preferences` 最低置信度偏好按校准值判定、且 `shouldAutoStore`/白名单代码零改动(代码评审 + `git diff` 断言)

## 5. 文档与全量验证

- [x] 5.1 GUIDE/README 增补决策接入章节:开关说明、环境变量 key 配置(不写真实值)、fail-open 语义、关闭即回退的运维路径,验证文档不含密钥样例
- [x] 5.2 跑 `pnpm typecheck`、`pnpm -w run lint`、`pnpm test` 三条全绿;手工冒烟:关闭开关跑 recall 与基线一致 → 开启 + 门控触发一次精排 → doctor 可见计数
