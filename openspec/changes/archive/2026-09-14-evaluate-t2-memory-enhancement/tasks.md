## 1. 评估基线与数据集

- [x] 1.1 固定当前 `xpi-memo + mnemosyne` baseline 的版本、配置、召回输出格式和指标，并用 `pnpm test` 验证现有行为未被改变
- [x] 1.2 构造可脱敏、可重复的中文场景集，覆盖用户偏好、项目连续性、中英混合技术和中文多跳语义，并通过人工检查确认每条样本有标准答案与证据来源
- [x] 1.3 定义 recall、误召回、证据完整性、延迟、资源占用和降级结果的记录格式，并用一组 baseline 样本验证报告可生成

## 2. 候选静态评估

- [x] 2.1 审阅 `memvid`、`memU`、`Memori`、`agentmemory` 的许可证、存储、依赖、中文 embedding、原文保留和删除能力，并交付候选矩阵
- [x] 2.2 检查四个候选是否支持本地/关闭云端模型、可回滚派生索引和零热路径模型调用，并为每个候选记录硬门槛结论
- [x] 2.3 评估候选的自我进化能力是否能覆盖用户画像、工作流、项目知识和技能，并用源码或官方文档引用支撑结论
- [x] 2.4 根据静态矩阵选出进入运行评测的候选，验证排序、淘汰理由和未安装约束已记录

## 3. 三档模型与关系评测设计

- [x] 3.1 定义无 embedding、本地 embedding、云端 embedding 三档执行协议，验证每档的隐私边界、延迟和失败行为可记录
- [x] 3.2 定义轻量证据图的有限边类型、来源引用、重排权重和关闭条件，并用示例验证图谱不能替代原始证据
- [x] 3.3 定义 T2 派生提案到现有候选生命周期的字段映射，验证 evidence、L0 event、候选状态和回滚引用完整

## 4. 状态可见性与降级设计

- [x] 4.1 设计捕获、候选、确认、拒绝、失败和降级的流光/灰度/不显示状态模型，并用状态表验证不包含记忆正文或敏感信息
- [x] 4.2 设计只在候选需要确认时打断用户的交互边界，并验证普通捕获和 T2 失败不会打开咨询框
- [x] 4.3 设计 T2 不可用时回退现有搜索链的诊断字段，验证 baseline recall 在 adapter 缺失或失败时仍可用

## 5. 评估报告与接入闸门

- [ ] 5.1 执行选定候选的真实中文场景 A/B 评测，验证输出包含质量、误召回、证据、延迟、隐私和降级结果
  - **本阶段有意不执行**（用户裁定）：`proposal.md:14` 明写「不安装候选」，与本条冲突；按 proposal 收口。记录见 `docs/evaluation-reports/t2/ab-evaluation-scope.md`，入口条件已列全。
- [x] 5.2 按严格治理门槛审查结果，验证任何直接写入、无证据派生、不可回滚或热路径模型依赖的候选均被拒绝
- [x] 5.3 交付评估报告和 adapter 接入建议；若无候选通过，验证结论明确保持现有系统不变；若有候选通过，另开实现变更并附 rollback 计划
