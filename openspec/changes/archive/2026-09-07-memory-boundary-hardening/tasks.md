## 1. 安全处理契约

- [x] 1.1 定义统一的召回/外发安全结果类型、固定策略版本、阻断/脱敏/拒绝原因和有界计数字段，并用类型检查验证旧调用方仍可编译
- [x] 1.2 实现高置信度提示注入规则与逐条阻断逻辑，覆盖英文/中文明显命令式模式，并用单元测试验证命中、非命中、混合批次和大小边界
- [x] 1.3 复用或提取现有凭证脱敏规则，生成独立外发安全副本；用单元测试验证常见 token、API key、密码脱敏及不确定内容拒绝

## 2. 召回与诊断接入

- [x] 2.1 接入自动召回渲染，使模型只收到安全格式化且带不可信边界的条目，并验证空结果不产生空记忆块
- [x] 2.2 接入 `xpi_memo_recall`、`xpi_memo_show_injected` 和 context preview（若当前入口存在），使它们复用同一安全契约，并验证安全条目、阻断数量和有界原因可见
- [x] 2.3 扩展 `memory_injected` 的可选诊断字段并保持旧事件默认值解析，验证历史事件读取、阻断/省略计数和策略版本不会记录正文
- [x] 2.4 接入非 TUI status/doctor 的安全计数和失败类别反馈，验证安全拒绝与 backend 无命中、路由失败、存储失败保持可区分

## 3. 外部边界与导出

- [x] 3.1 在 Track B runner、Mnemosyne 和其他 provider 调用前接入外发安全副本，验证已脱敏内容才发送、拒绝状态不发请求且不伪装成成功
- [x] 3.2 验证 L0 原始事件与外发安全副本分离，确保安全处理不回写历史事件；用回放/调用参数测试确认外部调用不读取原始凭证
- [x] 3.3 为 `MEMORY.md` 现有 privacy 脱敏路径补回归测试，验证导出不会绕过凭证保护；不修改投影游标和重建逻辑

## 4. 集成验证

- [x] 4.1 运行覆盖自动召回、显式工具、历史注入查询、外部 runner/provider、导出和非 TUI 诊断的回归测试，确认每条入口都经过统一安全契约
- [x] 4.2 运行 `pnpm typecheck`、`pnpm -w run lint` 和 `pnpm test`，确认实现与项目质量门禁全部通过
- [x] 4.3 检查变更范围和日志/事件输出，确认未新增依赖、未记录正文或凭证、未修改 UI change 和 L0/T1 一致性语义
