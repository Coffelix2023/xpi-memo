# DELTA.md — hifi ↔ wireframe 差异说明

> 结构继承 wireframe，视觉自由。**每条偏离线框的决策都必须登记在此**，未登记视为偏离。

| 日期 | 线框决策 | hifi 改动 | 原因 | 批准 |
| :--- | :--- | :--- | :--- | :--- |
| 2026-09-20 | 无上游线框 | 本版直接起稿 | 同项目没有 wireframe 产出 | — |
| 2026-09-20 | TUI 版：78×20 字符网格 | 800×600 像素窗口 | 媒介从终端字符换成 Glimpse webview；字符约束（折叠硬需求、body 15 行）不再存在 | 用户已确认 |
| 2026-09-20 | TUI 版：顶部 4 个 tab | 左侧 180px 垂直边栏 | 桌面面板惯例；`principles.md` 已写「sidebar 复刻 shadcn 外观」 | 用户已确认 |
| 2026-09-20 | TUI 版：固定 20px 行高 | 8pt 间距节奏 + 1.5 行高 | 像素媒介需要真实排版节奏，不是等宽字符网格 | 用户已确认 |
| 2026-09-20 | TUI 版：字符画 `█░▁▂▃` | 真像素占用条 + 8 级 SVG sparkline | 像素媒介不需要字符近似 | 用户已确认 |
| 2026-09-20 | TUI 版：40 列窄屏降级（10 条规则） | 无窄屏态 | 窗口固定 800×600，没有降级余地 | 用户已确认 |
| 2026-09-20 | TUI 版：ASCII 降级符号层 | 无 ASCII 层 | webview 不受终端编码影响，乱码问题不存在 | 用户已确认 |
| 2026-09-20 | 现役 `buildGlimpseHtml` 硬编码 GitHub-dark hex | 只用 THEMES.md 的 oklch 变量 | 项目 SSOT 是 THEMES.md；现役实现的 hex 与它冲突（见 plan.md §10.1） | 用户已确认 |
| 2026-09-20 | 现役 Glimpse 只有状态页 | 覆盖 4 页 | 用户选定「4 个 tab 全覆盖」 | 用户已确认 |
| 2026-09-20 | 现役状态页 JSON 区占半个屏 | 保留可滚动，位置与占比不变 | 对齐现役实现，信息不丢失 | 用户已确认 |

> 信息层级（哪些内容在前、有哪些 CTA）不可擅自变更；仅布局与视觉可在本节说明后调整。
| 2026-09-20 | 计划初稿：详情 380px | 实际 316px | 内宽 572 − 列表 240 − 间隔 16 = 316；初稿未扣 48px padding | 实现修正 |
| 2026-09-20 | 计划初稿：状态卡与设置行由 JS 生成 | 改为静态元素 + JS 只填文字 | JS 生成的 id 不在 HTML 源码里，57 个字典短码 grep 不到 | 实现修正 |
| 2026-09-20 | 计划初稿：设置页 500px 内放下全部 | 列表区 380px 独立滚动 + 详情 72px 固定 | 全展开 25 行 ≈ 1100px，必须滚动；详情固定才不随滚动消失 | 实现修正 |
